# Lint settings for the M5 project (run: cd m5 && ruff check src tests).
line-length = 130

[lint]
# Pinned explicitly so a newer ruff with different defaults gives the same result locally and in CI.
select = ["E4", "E7", "E9", "F"]
